# Local PostgreSQL
POSTGRES_PORT=55432
POSTGRES_DB=tamamori
POSTGRES_USER=tamamori
POSTGRES_PASSWORD=local-dev-only

# apps/api
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:${POSTGRES_PORT}/${POSTGRES_DB}
