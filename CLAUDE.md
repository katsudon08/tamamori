# CLAUDE.md

このリポジトリで作業する際の運用ルール。

## Git 運用

- **コミット・push・PR 作成は勝手に行わない。** 必ずユーザーの明示的な指示を受けてから実行する。計画(plan)がコミットを含んでいても、それだけでは実行の許可とはみなさない。
- ファイルの編集自体は指示の範囲で進めてよいが、その内容を確定させる git 操作は指示待ちとする。
- コミットメッセージ・PR・docs に `Co-Authored-By` 等の作者トレーラを付けない。
- commit / PR / issue で `--label` による自動ラベル付与は行わない(ラベルは手動運用)。
- PR のベースブランチは当面 `main`(`develop` は不在)。
